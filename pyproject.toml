[project]
name = "baseball-db"
version = "0.1.0"
description = "A baseball database"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-duckdb>=1.9.2",
    "duckdb>=1.2.0",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "sqlfluff>=3.3.1",
]
