[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
duckdb = "^0.10.2"
requests = "^2.31.0"
dbt-duckdb = "^1.8.0"
dbt-core = "^1.8.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
